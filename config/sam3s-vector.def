/* vim: filetype=c
 *
 * Atmel SAM3S{4,2,1} Datasheet
 */
/*
 * VECTOR(addr, name)
 * VECTOR_NULL(addr)
 * IRQ(address, name)
 * IRQN(num, sname, name)
 * IRQN_RESERVED(num)
 * IRQ_START_ADDR(addr)
 * VECTOR_END(addr, vect_ct, irq_ct)
 */
#include <config/armv7m-vector.def>
IRQ_START_ADDR(0x40)
#include <sam3/periph.def>
